dbt-snowflake>=1.9,<2.0
snowflake-connector-python>=3.12
yfinance>=0.2.40
pandas>=2.0

# Offline, zero-credential demo path only (DBT_TARGET=duckdb).
# Not used when the pipeline runs against Snowflake.
dbt-duckdb>=1.9,<2.0
duckdb>=1.0

# Two Claude API callers share these: the assisted-triage diagnosis step
# (scripts/triage/diagnose_failure.py) and the analytics agent (agent/). The
# agent also needs snowflake-connector-python, already pinned above. Kept in one
# file deliberately - a second requirements.txt under agent/ drifted to a weaker
# anthropic pin within a single release cycle.
anthropic>=0.70
python-dotenv>=1.0
